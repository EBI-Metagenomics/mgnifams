{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/EBI-Metagenomics/mgnifams/main/assets/schema_input.json",
    "title": "EBI-Metagenomics/mgnifams pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "protein_input": {
                "type": "string",
                "pattern": "^\\S+\\.(gz|bz2|csv|fa|fasta)$",
                "description": "The pipeline main input data. Either sequence_explorer_protein from MGnify proteins, or a protein fasta file with fasta_input_mode set to true.",
                "errorMessage": "Must be either sequence_explorer_protein from MGnify proteins, or a protein fasta file with fasta_input_mode set to true."
            }
        },
        "required": ["sample", "protein_input"]
    }
}
