profiles {
    slurm {
        process {
            withName: MMSEQS_CREATEDB { memory = '30.0 GB'; time = '30m' }
            withName: MMSEQS_LINCLUST { cpus = 16; memory = '190.0 GB'; time = '2h'
                ext.prefix = "mmseqs_families"
                ext.args   = '--min-seq-id 0.5 -c 0.9 --cov-mode 0 --remove-tmp-files 1 -v 0'
            }
            withName: CREATETSV       { cpus = 4; memory = '40.0 GB'; time = '10m' }
        }
    }
    local {
        process {
            withName: MMSEQS_LINCLUST { cpus = 8; memory = '10.0 GB'
                ext.prefix = "mmseqs_families"
                ext.args   = '--min-seq-id 0.5 -c 0.9 --cov-mode 0 --remove-tmp-files 1 -v 0'
            }
            withName: CREATETSV       { cpus = 8; memory = '2.0 GB' }
        }
    }
}
